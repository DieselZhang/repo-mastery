# npm — never commit auth tokens or local packages
.npmrc
node_modules/
*.tgz
*.log

# macOS
.DS_Store

# learning data created at runtime by the skill (never commit)
.learning/

# internal design docs (never commit)
.local/

# Python cache artifacts
__pycache__/
*.pyc
.pytest_cache/
